# libtmux-mcp starts its dedicated server without loading user hooks or plugins.
set -g exit-empty off
set -g status off
set -g @libtmux_mcp_owner "$LIBTMUX_MCP_OWNER"
set-environment -gu LIBTMUX_MCP_OWNER
